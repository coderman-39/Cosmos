# Test/dev dependencies (not needed to run FRIDAY, only to run the test suite)
# Install:  .venv/bin/python3 -m pip install -r requirements-dev.txt
# Run:      .venv/bin/python3 -m pytest -q
pytest>=8.0
pytest-asyncio>=0.23
